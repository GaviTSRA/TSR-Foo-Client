# Alpha v1.0
-Client side js -> any message starting with '?cjs' will be executed
-[UNTESTED]Update button Foo -> TSR-Foo
 (Requires reset button(Settings -> TSR Client -> Reset update url) to be pressed)

-Settings Menu:
    -Name field
    -UUID field
    -Enable client side js box
    -Reset update url button

-Pause Menu:
    -Reconnect button

# Alpha v2.0
-Settings Menu:
    -Make all blocks visible box
-Pause Menu:
    -Profile Manager:
        -Use profile
        -Add Profile
        -Delete profile
    -Random profile button